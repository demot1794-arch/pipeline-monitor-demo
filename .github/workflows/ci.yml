name: CI Pipeline

on:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:

      - name: Create Report
        run: |
          echo "Pipeline Success" > report.txt

      - name: Upload Artifact
        uses: actions/upload-artifact@v4
        with:
          name: pipeline-report
          path: report.txt
